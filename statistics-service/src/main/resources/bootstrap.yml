spring:
  cloud:
    config:
      name: rate
      profile: config
      discovery:
        enabled: true
        serviceId: config
    bus:
      trace:
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://luhost:Test_12345@localhost:26335/eureka/